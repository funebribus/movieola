<%= form_for(@respost) do |f| %>
  <% if @respost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@respost.errors.count, "error") %> prohibited this respost from being saved:</h2>

      <ul>
      <% @respost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :corpo %><br>
    <%= f.text_area :corpo %>
  </div>
  <div class="field">
    <%= f.label :quantidadelikes %><br>
    <%= f.number_field :quantidadelikes %>
  </div>
  <%= f.hidden_field :topico_id %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
